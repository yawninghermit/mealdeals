# Cloudflare Turnstile site key (get this from the Cloudflare dashboard)
# Must match the secret key configured in your Supabase project Auth settings
VITE_TURNSTILE_SITE_KEY=your_turnstile_site_key_here
